<Window x:Class="WhatSize.Window1"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="WhatSize" Height="300" Width="300"
	>
	<Grid Name="grid">
		<StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
				VerticalAlignment="Center">
		<!-- TextBlock>
			<Run Text="{Binding ElementName=grid, Path=ActualWidth, Mode=OneWay}" />
			<Run Text=" &#x00D7; " />
			<Run Text="{Binding ElementName=grid, Path=ActualHeight, Mode=OneWay}" />
			<Run Text=" device independent units." />
		</TextBlock -->
		<TextBlock Text="{Binding ElementName=grid, Path=ActualWidth, Mode=OneWay}" />
		<TextBlock Text=" &#x00D7; " />
		<TextBlock Text="{Binding ElementName=grid, Path=ActualHeight, Mode=OneWay}" />
		<TextBlock Text=" device independent units." />
		</StackPanel>
	</Grid>
</Window>